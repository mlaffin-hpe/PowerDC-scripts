### Instructions to run the setup script:

1. Clone repository to workstation
2. Open Sigrity PowerDC and checkout a license
3. Open TCL Command Pane (View->Pane->TCL Command)
3. In the command input field, source the script file:

```
source "C:/work/PowerDC-scripts/setup.tcl"
```
